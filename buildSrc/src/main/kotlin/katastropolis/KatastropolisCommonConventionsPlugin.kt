package katastropolis

import org.gradle.api.Plugin
import org.gradle.api.Project

/**
 * Placeholder convention plugin for the katastropolis build.
 * Currently, a no-op; extend later as needed.
 */
class KatastropolisCommonConventionsPlugin : Plugin<Project> {
  override fun apply(target: Project) {
    // no-op for now
  }
}
